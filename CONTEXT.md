# Radar

Radar 是一个开源、本地运行的个性化信号聚合站。用户用自然语言描述想持续知道什么，Radar 据此持续采集来源内容、排成待判断队列，并保存判断、反馈与交付历史。全部 AI 工作由用户自己的 Agent 通过 Radar Skill 承担——Radar 自身不调用任何模型，也不拥有知识沉淀。

## Language

### 产品与角色

**Radar**:
按 Radar Brief 持续采集外部来源内容、维护待判断队列、保存判断与反馈历史的开源、本地运行信号聚合站。它不调用模型，不判断相关性，也不生成报告。
_Avoid_: 新闻聚合器、爬虫、AI 助手

**Radar 实例**:
用户从开源仓库在自己控制的设备或自托管环境中运行的一套 Radar。默认单用户、本地数据，实例内可以有多个 Radar Brief，来源凭据与全部历史由该用户掌管，不依赖 Radar 账号。
_Avoid_: Radar 账号、Radar 云端

**Agent**:
用户自己的 AI Agent，通过 Radar Skill 承担 Radar 的全部 AI 工作：按 Brief 判断来源内容、取已判断的内容出报告或写入用户知识库、把用户反馈写回。它同时是用户操作 Radar 的主入口。Claude Code、Codex、Hermes、OpenClaw、Pi 都可以承担这个角色。
_Avoid_: Radar 内置 AI、Hermes

**Radar Skill**:
随 Radar 分发、装进用户 Agent 的一组技能，定义 Agent 与 Radar 之间的读写操作面，分判断角色（取待判断队列、写回判断）与取数角色（取已判断内容用于输出）。
_Avoid_: Radar API 文档、插件、集成

**用户知识库**:
Radar 之外、由用户自己拥有的长期沉淀去处，例如 Obsidian Vault、Notion Workspace 或 llm-wiki。Agent 向它写入，Radar 不拥有、不连接、不同步，也不知道它的存在。
_Avoid_: 情报库、知识库连接器、外部知识库

### Brief 与判断

**Radar Brief**:
Agent 根据用户的自然语言描述整理出的关注目标、重点对象、判断标准、排除项与交付偏好；它既是判断依据，也是 Radar 内的空间单位——每个 Brief 独立拥有自己的来源偏好、待判断队列、判断历史、反馈与交付记录。只有用户明确的自然语言修正或对修订建议的采纳才能改变它。
_Avoid_: Radar Project、Radar 任务、筛选条件、关键词配置

**Radar Brief 修订**:
Radar Brief 在用户明确修正目标或采纳建议后形成的一次可追溯变化；当前版本与历史版本同时保留，并记录变化依据。
_Avoid_: 偏好漂移、自动画像更新

**来源内容**:
Radar 采集到、尚未被任何 Radar Brief 判断过的外部内容。同一份来源内容可以进入多个 Brief 的待判断队列，各自独立判断。
_Avoid_: Signal、判断、文章

**待判断队列**:
某个 Radar Brief 已采集、等待 Agent 判断的来源内容集合，按该 Brief 的排队策略排序。Radar 只负责入队、去重与排序，不判断相关性，也不用任何规则丢弃内容——未被判断的内容留在队列里，超过保留窗口后移出队列但不删除，可被显式回捞。用户 Agent 不运行时队列继续堆积。
_Avoid_: 收件箱、Feed、资讯列表

**排队策略**:
Agent 为某个 Radar Brief 写下、由 Radar 机械执行的一组带权重的信号，决定待判断队列的顺序。信号只取 Radar 自己就有的机械事实——新鲜度、端点权重、平台自带热度、关键词命中、该端点在本 Brief 的历史命中率——Radar 据此算分排序，全程不理解内容。策略只改变先看哪条，不改变什么算相关；Agent 可依据 Radar 的命中统计自行修订，修订可追溯。
_Avoid_: 筛选规则、相关性算法、偏好模型、隐式兴趣

**判断**:
Agent 依据某个 Radar Brief 对一份来源内容作出的一次判定，包含相关与否、给用户的理由与所引用的 Signal。判断以这一次判定为身份：Radar 不合并、不修订、不跨判断汇总，同一件事的沉淀由用户知识库承担。
_Avoid_: 情报条目、文章、卡片、Report 条目

**Signal**:
被某个判断引用为证据的来源内容。
_Avoid_: 证据、原始帖子、候选内容

**反馈**:
用户明确对某个判断或某类内容表达的处置与原因，由 Agent 通过 Radar Skill 写回 Radar，作为后续判断的上下文。只有用户明说的才是反馈；Agent 自己在取用时的取舍不构成反馈。
_Avoid_: 隐式兴趣、点赞、观察行为、噪音分数

**交付记录**:
某个判断已被 Agent 取走用于输出或写入用户知识库的记录；它说明什么已经交付过，不保存输出本身。
_Avoid_: Report、平台物料包、生成运行

**自然语言修正**:
用户在与 Agent 的对话中明确改变 Radar Brief、来源配置或对某个判断的处置的指令；Agent 将其解析为具体、可追溯的 Radar 变化，语义含混或作用域广泛时先请求确认。
_Avoid_: 隐藏提示词、聊天备注

### 来源与场景

**采集渠道**:
一类外部来源的适配器类型——怎么调、要什么凭据、由谁执行采集。渠道本身不可采集，它下面的采集端点才可采集。每个渠道处于三种配置状态之一：**装好即用**（无需凭据，由 Radar 自己按计划采集）、**配置后解锁**（用户配好凭据后，由用户 Agent 采集并推送给 Radar）、**够不着**（当前没有任何后端能到达）。
_Avoid_: 平台、数据源、爬虫

**采集端点**:
某个采集渠道下一个具体可被采集的对象——一个 feed 网址、一个仓库、一个 subreddit、一个 UP 主。它是采集、去重、来源状态与开关的实际单位；配置后解锁的渠道也须先在 Radar 登记端点，Agent 推送的内容才有来源归属。
_Avoid_: 来源、订阅、Source

**出厂来源目录**:
Radar 随版本自带的采集端点清单，按采集渠道分组。装好即用渠道下的端点在实例装好后即开始采集，用户不做任何配置也能收到来源内容；配置后解锁的渠道摆在目录里并标明怎么配，够不着的渠道灰着摆在那里，作为覆盖缺口的表达。爱折腾的用户在此之上横向扩展：粘一个网址进来，Radar 依次尝试匹配 RSSHub 规则库、探测页面自带的 feed、交给认得该域名的渠道适配器，都不中就明示够不着，不降级去抓 HTML。
_Avoid_: 用户订阅列表、Source Network

**来源授权**:
用户授予某个 Radar 实例访问特定采集渠道的方式与范围。撤销后实例停止采集该渠道下的所有端点，但不删除已经取得的来源内容与判断历史。OAuth、API token、Cookie 或数据库凭据属于本地 secret，不进入领域数据或完整导出。
_Avoid_: Radar 登录、产品账号授权、发布授权

**完整导出**:
用户可从一个 Radar 实例带走的档案，包含 Radar Brief 及其修订、来源内容、判断、反馈与交付记录，并同时提供可机读结构与可直接阅读的内容。单个 Brief 的导出不依赖其他 Brief 或运行中的实例即可阅读。
_Avoid_: 数据库备份、知识库导出

**Demand Radar**:
Radar 的首个 Brief 场景，关注高呼声、正在变化且可能尚未被充分满足的需求与痛点。
_Avoid_: 通用趋势榜、竞品监控

## 待收口

`来源状态`（采集端点的运行时状态与呈现）由 [#44](https://github.com/2093686099/radar/issues/44) 收口；`关注主题`与`关注对象`的划分由 [#42](https://github.com/2093686099/radar/issues/42) 收口。两者收口后写入本文件。
